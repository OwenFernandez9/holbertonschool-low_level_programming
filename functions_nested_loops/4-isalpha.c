#include "main.h"
/**
 * _islower - function is used to check lowercase letter
 *
 * @c: character check
 *
 * Return: c is 1 and letters is 0
 */
int _isalpha(int c)
{
	return (c >= 'A' && c <= 'Z');
}
